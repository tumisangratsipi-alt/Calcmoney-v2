/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "standalone", // Configured for a native Node.js hosting environment (Hostinger VPS / App Hosting)
};

export default nextConfig;
